import React from 'react';
import useTitle from '../../hooks/useTitle';

const Contact = () => {
    useTitle('Contact')
    return (
        <div>
            Contact Us
        </div>
    );
};

export default Contact;