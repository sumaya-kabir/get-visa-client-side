import React from 'react';
import useTitle from '../../hooks/useTitle';

const Services = () => {
    useTitle('Services')
    return (
        <div>
            
        </div>
    );
};

export default Services;